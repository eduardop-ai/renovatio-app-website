Drop the synthetic screenshot PNGs here, generated by the app repo's
`Website/render_shots.py` from `carlos/demo_data.py`:

- `launcher-light.png` / `launcher-dark.png` (500×1420) — the six modules, no data
- `launcher-wide.png` (1280×1000) — same, desktop width
- `carlos-light.png` / `carlos-dark.png` (500×2100) — the C.A.R.L.O.S. module, synthetic data
- `carlos-wide.png` (1280×1500) — same, desktop width

Never a real screenshot of the app — every one of these must be rendered from synthetic data
only. Once added, wire them into the `#screens` section of `index.html` in place of the
"pending" placeholders.
