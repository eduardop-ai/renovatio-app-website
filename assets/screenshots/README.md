Synthetic screenshot PNGs, generated by the app repo's `Website/render_shots.py` from
`carlos/demo_data.py`. Already wired into the `#screens` section of `index.html`.

- `launcher-light.png` / `launcher-dark.png` (500×1420) — the six modules, no data
- `carlos-light.png` / `carlos-dark.png` (500×2100) — the C.A.R.L.O.S. module, synthetic data
- `carlos-wide.png` (1280×1500) — same, desktop width, light mode only

`launcher-wide.png` (1280×1000) hasn't been supplied yet — not currently used on the site.

Never a real screenshot of the app — every one of these must be rendered from synthetic data
only. If you regenerate them, drop the new files in here with the same names; no HTML changes
needed unless the set of files changes.
